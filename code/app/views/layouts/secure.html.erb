<% content_for :header do%>
<div class="logout">
	<b>Hi, <%= current_user.name %>!</b> (<%= link_to('Log out', destroy_user_session_path, :method => :delete) %>)
</div>
<%end%>

<% content_for :content do %>
<div id="talltabs-apple">
	<ul>
		<% if current_user.is_admin?%>
		<li>
			<%= link_to "Home", {:controller=>'adminhome', :action=>'index'} %>
		</li>
		<li>
			<%= link_to "Manage Employees", {:controller=>'manageusers', :action=>'index'} %>
		</li>
		<% else %>
		<li>
			<%= link_to "Home", {:controller=>'userhome', :action=>'index'} %>
		</li>
		<%end%>
		<li>
			<%= link_to "Manage Requests", {:controller=>'requests', :action=>'index'} %>
		</li>
	</ul>
</div>
<%= yield %>
<% end %>

<%= render :file => 'layouts/application' %>